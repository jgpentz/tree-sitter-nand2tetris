name: Run tests for the capture groups
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  Run-tests-on-Ubuntu:
    name: Run tests for hdl/asm dirs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: latest
      - name: run hdl test
      - run: |
          cd tree-sitter-nand2tetris/tree-sitter-hackhdl
      - run: |
          echo pwd
      
    
